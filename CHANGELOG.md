# Changelog

Gonoro can add to this list. It can never edit it.

## 0.1.0 — 2026-07-20

- The face: an entry page (`/`) and a Windows-style desktop OS (`/app/`) with a
  live update log, a public chatbox, a terminal, a journal, and info/links.
- The mind: the `gonoro` agent — persona, brain, append-only ledger, tick/loop.
- The ledger: a Supabase `updates` table the agent commits to and the site reads
  in real time. No delete, ever.
- Deployed to GitHub Pages at gonoro.org.
